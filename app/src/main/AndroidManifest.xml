<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hasyim.pertemuanpertama" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PersamaanActivity" />
        <activity android:name=".InputActivity" />
        <activity
            android:name=".ContohActivity"
            android:label="@string/title_activity_contoh" >
        </activity>
        <activity
            android:name=".UtsActivity"
            android:label="@string/title_activity_uts" >
        </activity>
        <activity
            android:name=".BmiActivity"
            android:label="@string/title_activity_bmi" >
        </activity>
    </application>

</manifest>
